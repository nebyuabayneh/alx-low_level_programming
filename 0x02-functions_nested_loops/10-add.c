#include <unistd.h>
#include "main.h"

/**
 * int add - Check the code
 * Description: return the sum of two numbers
 * Return: Always 0.
 */
int add(int a, int b)
{
    int sum;
    sum = add(a,b);
    printf("%d\n", sum);
    return(0);
}
