#include <stdio.h>
#include <string.h>

/**
 * _strcpy - check the code
 * @dest: pointer char
 * @src: pointer char
 * Return: char
 */

char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
